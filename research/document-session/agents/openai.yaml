interface:
  display_name: "Document Research Session"
  short_description: "Capture worklogs and immutable research handoffs"
  default_prompt: "Use $document-session to checkpoint or hand off the current research task with evidence."
policy:
  allow_implicit_invocation: false
