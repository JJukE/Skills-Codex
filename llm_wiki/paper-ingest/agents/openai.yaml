interface:
  display_name: "Paper Ingest"
  short_description: "Ingest Zotero papers into an LLM Wiki"
  default_prompt: "Use $paper-ingest to create a Zotero paper seed note, connect official code if available, and compile the paper into an LLM Wiki."

policy:
  allow_implicit_invocation: true
